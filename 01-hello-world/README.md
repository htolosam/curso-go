# Hello World

El primer programa en cualquier lenguaje de programación.

## Ejecutar

```bash
go run main.go
```

## Conceptos

- `package main`: Define un paquete ejecutable
- `import`: Importa paquetes externos (fmt para formateo)
- `func main()`: Punto de entrada del programa
- `fmt.Println()`: Imprime texto en la consola
